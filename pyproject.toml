[project]
name = "fastapi_skeleton-generator"
version = "0.1.0"
description = "This script will generate you fastapi petproject skeleton (sync)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
